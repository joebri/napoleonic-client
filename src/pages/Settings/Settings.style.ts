import { css } from '@emotion/react';

const classes = {
  container: css({
    display: 'flex',
    flexDirection: 'column',
    margin: '0.5rem auto 2rem auto',
    width: '80%',
  }),
  actionBar: css({
    marginTop: '1rem',
  }),
};

export { classes };
