import { css } from '@emotion/react';

const classes = {
  container: css({
    display: 'flex',
    flexDirection: 'column',
    margin: '1rem 1rem',
  }),
  actionBar: css({
    marginTop: '1rem',
  }),
};

export { classes };
