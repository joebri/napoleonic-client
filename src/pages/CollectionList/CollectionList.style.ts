import { createStyles } from 'createStyles';

const classes = createStyles({
  container: {
    gap: '1rem',
    display: 'flex',
    flexDirection: 'row',
    flexWrap: 'wrap',
    margin: '2rem auto 2rem auto',
    width: '85%',
  },
});

export { classes };
