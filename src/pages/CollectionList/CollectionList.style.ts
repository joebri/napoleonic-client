import { css } from '@emotion/react';

const classes = {
  container: css({
    padding: '0 2rem 2rem 2rem',
  }),
  title: css({
    alignItems: 'center',
    display: 'flex',
    margin: '1rem 0 0.5rem 0',
    whiteSpace: 'break-spaces',
  }),
  button: css({
    marginTop: '1rem',
  }),
};
export { classes };
