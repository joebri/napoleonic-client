import { css } from '@emotion/react';

const classes = {
  container: css({
    padding: '0 2rem 2rem 2rem',
  }),
  title: css({
    alignItems: 'center',
    display: 'flex',
    whiteSpace: 'break-spaces',
  }),
  button: css({
    marginTop: '1rem',
  }),
};
export { classes };
