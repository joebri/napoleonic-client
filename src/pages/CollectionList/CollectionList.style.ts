import { css } from '@emotion/react';

const classes = {
  container: css({
    margin: '2rem auto 2rem auto',
    width: '85%',
  }),
  button: css({
    marginTop: '1rem',
  }),
};
export { classes };
