import { css } from '@emotion/react';

const classes = {
  container: css({
    margin: '0.5rem auto 2rem auto',
    width: '80%',
  }),
  message: css({
    color: 'red',
  }),
};

export { classes };
